import {Header, Segment} from 'semantic-ui-react';
import React from 'react';

const General = () => {
    return (
        <Segment color={'grey'} loading={false}>
            <Header as={'h1'}>General</Header>
        </Segment>
    );
};

export default General;
