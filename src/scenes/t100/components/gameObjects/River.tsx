import { Image } from 'semantic-ui-react';
import river from '../../assets/river.png';

const River = () => {
  return <Image src={river} style={{ cursor: 'pointer', left: 109, top: 0, zIndex: 999 }} />;
};

export default River;
